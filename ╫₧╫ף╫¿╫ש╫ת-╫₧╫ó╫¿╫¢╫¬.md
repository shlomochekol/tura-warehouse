# מערכת ניהול מחסן — יקב טורא · מדריך מערכת

מסמך אחד שמסביר מה המערכת, איך מפעילים אותה, ומה עושים כשמשהו נשבר.
עודכן: יולי 2026.

---

## 1. מה זו המערכת

אפליקציית ניהול מחסן והפצה ליקבים ובתי בד. שבעה מסכים:

| מסך | תפקיד |
|---|---|
| **דשבורד** | תמונת מצב, הרכב מלאי, התראות מלאי נמוך, לוח משלוחים |
| **מפת מחסן** | תצוגה חזותית; כל תא = מיקום, עד 3 מפלסים. גרירה מזיזה משטח. לחיצה על משבצת פנויה ("+ שבץ") משבצת פריט ללא מיקום |
| **מיקום במחסן** | **מקור האמת** — כאן מוסיפים ועורכים מלאי |
| **משימות** | רשימת משימות תפעולית + לוח שבועי/חודשי לכוח אדם |
| **מלאי מחסן** | סיכום נגזר אוטומטית ממיקום במחסן (תצוגה בלבד) |
| **יומן תנועות** | תיעוד אוטומטי של כל שינוי בכמות פריט — לפני/אחרי/סיבה (עריכה ידנית / ניכוי ממשלוח / החזרה / ייבוא). סינון לפי תאריך, קטגוריה וסוג פעולה |
| **משלוחים** | טבלת משלוחים עם חישוב ארגזים/משטחים |
| **הפצה** | ייבוא מסלול מ-LionWheel, מדבקות עם ברקוד, סריקת העמסה, **דף ליקוט**, מסלולים שמורים |

**עקרון מרכזי:** מסך "מיקום במחסן" הוא המקור; כל שאר המסכים נגזרים ממנו.

---

## 2. ארכיטקטורה

```
[דפדפן: index.html]  ←→  [Supabase: מסד נתונים + התחברות]
        ↑
   [Netlify: אחסון האתר]
```

- **index.html** — שלד הממשק בלבד (~24KB). הקוד מפוצל למודולים תחת `assets/`:

```
assets/app.css            עיצוב
assets/data/seed.js       נתוני הבסיס
assets/data/products.js   קטלוג ברקודי המוצרים (122)
assets/js/21-picking.js   דף ליקוט + היסטוריית מסלולים
assets/js/22-supply.js    אספקה משילה
assets/js/01-core.js      קבועים, state, חישובים (ארגזים/משטחים)
assets/js/07-helpers.js   עזרים משותפים
assets/js/02-dashboard.js … 20-tasks.js   מודול לכל מסך/נושא
assets/js/99-boot.js      קוד ההפעלה (רץ אחרון)
assets/lib/               ספריות חיצוניות
```

**סדר הטעינה חשוב** — הקבצים נטענים לפי המספור. מודול חדש מתווסף לפני `99-boot.js`.

**טעינה עצלה:** שלוש הספריות הכבדות (Excel, סריקת ברקוד, ייצוא תמונה — יחד ~690KB) נטענות **רק כשמשתמשים בהן**. הטעינה הראשונית היא ~440KB במקום 1.1MB.

**עריכה בטוחה:** משנים מודול אחד בלבד; אין צורך לגעת בשאר. שמות פונקציות כפולים בין מודולים הם הסיכון העיקרי — בדיקת התקינות (🩺) מזהה אותם.

- **index-קובץ-בודד-לחירום.html** — עותק שמכיל הכל בקובץ אחד. לא בשימוש רגיל; נועד למצב חירום (לפתוח מקומית בלי שרת).
- **Netlify** — מארח את האתר. פריסה = גרירת תיקייה.
- **Supabase** — מסד הנתונים, ההתחברות, והגיבויים. פרויקט: `ncgehwgatdafxaydmhjc`.
- **LionWheel** — מקור חיצוני; מייבאים ממנו קובץ Excel של מסלול הפצה (ייצוא ידני).

**מצב עבודה כפול:** אם המשתמש מחובר לחשבון — הנתונים בענן Supabase. אם לא — האפליקציה עובדת מקומית בלבד (localStorage) ואפשר לחבר גיליון Google כגיבוי ישן.

---

## 3. מודל הנתונים

בענן יש טבלאות: `businesses`, `memberships`, `inventory`, `shipments`, `labels`, `inv_log`, `app_state`, `backups`.
כל שורה נושאת `business_id`, וכללי RLS מוודאים שעסק אחד לא רואה נתונים של עסק אחר — הבידוד נאכף במסד עצמו.

`inv_log` הוא יומן תנועות המלאי (מסך "יומן תנועות") — יש להריץ פעם אחת את `inv_log_setup.sql` ב-SQL Editor כדי ליצור אותו לפני שהיומן יתחיל להסתנכרן לענן; בלי זה היומן ימשיך לעבוד מקומית בלבד.

הנתונים נשמרים כ-JSON בעמודת `data`. כדי להריץ שאילתות ודוחות, הריצו פעם אחת את `views.sql` — הוא יוצר תצוגות (`v_inventory`, `v_shipments`, `v_labels`, `v_stock_summary`) שמציגות את המידע כעמודות רגילות; `v_inv_log` (בקובץ `inv_log_setup.sql`) עושה זאת ליומן התנועות.

**חוקי חישוב:**
- יחידות → ארגזים: לפי קטגוריה, ובנפרד למלאי מתוות/לא מתוות (מסך מיקום במחסן → 📦 כמויות לפי קטגוריה).
- ברירת מחדל 12 ליחידה; MP וויקטורי: 12 לא-מתוות / 6 מתוות, 90 ארגזים למשטח.
- בייבוא מ-LionWheel: מוצרים שאינם יין/שמן (בקבוקים ריקים, ארגזים, פקקים) נספרים 1 ליחידה. ניתן לשנות במסך הפצה → 📦 יחידות בארגז.
- ארגזים → משטחים: לפי קטגוריה, ברירת מחדל 75.
- מיקום אחד = משטח אחד לכל היותר.

**ברקודים:** הקובץ `assets/data/products.js` מכיל את קטלוג המוצרים (שם, ברקוד, ולמוצרי יין גם קטגוריה/בציר/סוג). הברקוד מוצמד אוטומטית לפריטי המלאי לפי הצירוף קטגוריה+בציר+סוג; ארכיון חולק ברקוד עם יין שיווק (אותו בקבוק). פריט שאין לו התאמה בקטלוג נשאר ללא ברקוד. לעדכון הקטלוג — לערוך את הקובץ ולהעלות מחדש.

---

## 4. פריסה ועדכון גרסה

1. לחלץ את ה-zip שקיבלת (`יקב-טורא-מחסן-app`).
2. להיכנס ל-app.netlify.com → האתר → לשונית **Deploys**.
3. לגרור את **התיקייה המחולצת** לאזור הגרירה. הכתובת נשארת זהה.
4. לרענן באפליקציה (ובטלפון).
5. **מיד אחרי עדכון:** ⚙ הגדרות → הגדרות מתקדמות → **🩺 הרץ בדיקה**. אם הכל ירוק — העדכון תקין.

⚠️ **חשוב לכל עדכון קוד:** האפליקציה מותקנת כ-PWA עם Service Worker ששומר את הקבצים במטמון של המכשיר ומגיש אותם *במקום* לפנות לרשת. אם קובץ ה-`sw.js` לא השתנה בעדכון — המכשיר ימשיך להציג את הגרסה הישנה גם אחרי שהאתר עודכן ב-Netlify, וזה נראה כאילו "התיקון לא נכנס". הפתרון: בכל עדכון שמשנה קובץ (JS/CSS/HTML), חייבים להעלות את המספר במשתנה `CACHE` בראש `sw.js` (למשל `mwe-v8` ל-`mwe-v9`). אחרי זה מספיק רענון רגיל של הדף (F5) כדי לקבל את הגרסה החדשה — לא צריך לנקות נתוני אתר.

**חזרה לגרסה קודמת:** ב-Netlify → Deploys → לבחור פריסה קודמת → **Publish deploy**. חוזר תוך שניות.

---

## 5. גיבוי ושחזור

- **אוטומטי:** גיבוי יומי לענן בכניסה לאפליקציה. נשמרות 10 הגרסאות האחרונות.
- **ידני:** ⚙ הגדרות → מתקדמות → **גבה עכשיו**.
- **שחזור:** אותו מקום → **🕘 שחזור גרסה קודמת** → בחירת גרסה. לפני כל שחזור נוצר גיבוי בטיחות אוטומטי.
- **גיבוי לקובץ:** ⚙ הגדרות → **ייצוא גיבוי (JSON)**. מומלץ פעם בשבוע לקובץ מקומי.

---

## 6. תקלות נפוצות

| תופעה | סיבה וטיפול |
|---|---|
| "שגיאת ענן" בשמירה | פג תוקף ההתחברות — המערכת מחדשת לבד; אם לא, להתחבר מחדש |
| המצלמה לא נפתחת | הסריקה עובדת רק מכתובת https (Netlify), לא מקובץ מקומי. לאשר הרשאת מצלמה |
| ברקוד לא נסרק | להדפיס מחדש — מדבקות ישנות עשויות לשאת קוד שכבר לא קיים |
| ברקוד בקבוק לא נקרא | ליישר את הברקוד למסגרת האדומה, להרחיק ~15 ס"מ, ולהדליק פנס בתאורה חלשה. אם עדיין לא — "⌨ הקלדה" |
| "הלקוח לא נמצא ברשימה" | מדבקה מהדפסה ישנה; המערכת מציעה לשייך ידנית |
| הדפסה יוצאת חתוכה | לכוון גודל מדבקה (מ"מ) במסך הפצה; בחלון ההדפסה: שוליים "ללא", קנה מידה 100% |
| נתונים לא מתעדכנים במכשיר שני | ללחוץ ☁︎ סנכרן; הסנכרון אינו מיידי ברמת השנייה |
| משהו נראה שבור אחרי עדכון | 🩺 הרץ בדיקה → אם יש כשל, לחזור לפריסה קודמת ב-Netlify |

---

## 7. ניהול לקוחות (SaaS)

**הרשמה:** כל משתמש שנרשם מקבל עסק חדש ריק, אוטומטית.

**אימות מייל:** מומלץ להפעיל — Supabase → Authentication → Providers → Email → **Confirm email**. בלי זה כל אחד יכול לפתוח חשבון.

### היכן ההגדרות
כל ההגדרות הרגישות נמצאות ב-**⚙ הגדרות → ⚙ הגדרות מתקדמות** (סגור כברירת מחדל):
- 📦 כמויות לפי קטגוריה · 🎨 קפסולות וצבעים — ⚠️ משפיעות על כל החישובים
- 🕘 שחזור גרסה קודמת · גבה עכשיו
- 🩺 בדיקת תקינות
- 🗺️ **איפוס מיקומים** — מנקה את השיבוץ במפה (שורה/עמודה/מפלס) לשיבוץ מחדש. אפשר לסנן לפי סוג/קטגוריה. **המלאי עצמו נשמר** — רק המיקום מתרוקן. נוצר גיבוי אוטומטי לפני הפעולה.
- איפוס לנתוני התחלה

**מנויים (ידני, בשלב ראשון):** בטבלת `businesses` יש `plan` ו-`trial_ends`.
- `trial` — ברירת מחדל, 14 יום. האפליקציה מציגה כמה ימים נותרו.
- `active` — לקוח משלם (לעדכן ידנית אחרי קבלת תשלום).
- `blocked` — חסום; מוצגת הודעה, הנתונים נשמרים.

לעדכון: Supabase → Table Editor → `businesses` → לשנות את `plan`.

---

## 8. המשכיות — מה לעשות אם המפתח לא זמין

**מה שחייבים לדעת:**
1. הקוד כולו נמצא ב-`index.html` היחיד — אין תלות בשרת חיצוני מלבד Supabase.
2. גיבוי הנתונים: Supabase → Table Editor → `backups` (JSON מלא של כל המערכת).
3. חזרה לגרסה קודמת: Netlify → Deploys → Publish deploy.
4. במקרה חירום מוחלט: לפתוח את `index-קובץ-בודד-לחירום.html` מקומית בדפדפן ולייבא גיבוי JSON — המערכת תעבוד בלי ענן ובלי שרת.

**מה לשמור במקום בטוח:** סיסמת Supabase, גישה לחשבון Netlify, וגיבוי JSON עדכני.

⚠️ **המפתח הסודי של Supabase (`service_role`) לעולם לא נכנס לאפליקציה ולא משותף.** רק המפתח הציבורי (`publishable`) נמצא בקוד, והוא בטוח.

---

## 9. נהלים מומלצים

- **שבועי:** ייצוא גיבוי JSON למחשב.
- **אחרי כל עדכון גרסה:** להריץ 🩺 בדיקת תקינות.
- **פיצול מיקומים** (>משטח אחד) יוצר פריט חדש **ללא מיקום** — משבצים אותו במפה דרך "+ שבץ". האפליקציה לא משבצת לבד.
- **הגדרות הכמויות נמצאות בהגדרות מתקדמות בכוונה** — שינוי שם משנה בשקט את כל ספירת המשטחים, ההתראות והמשלוחים. לשנות רק לאחר אימות מול המציאות.
- **מדבקות:** להדפיס תמיד מהרשימה הנוכחית, לא ממסמך ישן.
- **עבודה במקביל משני מכשירים בטוחה:** מלאי, משלוחים, מדבקות, משימות ולוח כוח האדם ממוזגים לפי פריט — כל אחד רואה גם את מה שהשני כתב. רק אם **שניים עורכים בדיוק את אותו פריט** בו-זמנית, האחרון ששמר מנצח (שאר הפריטים לא נפגעים).
